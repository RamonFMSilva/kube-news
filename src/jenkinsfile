pipeline {
    agent any

    stages {

        stage ('Build Docker image') {
            steps {
                dockerapp = docker.duild("ramonfmsilva/kube-news:${env.BUILD_ID}", '-f ./src/Dockerfile ./src')
            }    
        } 
    }
}